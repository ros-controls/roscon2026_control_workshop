# noVNC overlay for docker-compose.yaml. Starts a virtual X display (Xvfb),
services:
  workshop:
    environment:
      - USE_HOST_DISPLAY=${USE_HOST_DISPLAY:-0}
      - VNC_PORT=5900
      - NOVNC_PORT=6080
      - SCREEN_GEOMETRY=1920x1080x24
