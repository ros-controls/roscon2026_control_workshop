<?xml version='1.0' encoding='UTF-8'?>
<root>
 <tabbed_widget name="Main Window" parent="main_window">
  <Tab containers="1" tab_name="Introspection">
   <Container>
    <DockSplitter count="3" orientation="-" sizes="0.333653;0.332694;0.333653">
     <DockArea name="CM">
      <plot flip_x="false" flip_y="false" line_width="1.0" mode="TimeSeries">
       <range bottom="-1227.341000" left="51.220927" right="101.196346" top="50462.857000"/>
       <limitY/>
       <curve color="#1f77b4" name="/controller_manager/statistics/values/controller_manager.total_time"/>
       <curve color="#d62728" name="/controller_manager/statistics/values/controller_manager.update_time"/>
       <curve color="#1ac938" name="/controller_manager/statistics/values/controller_manager.read_time"/>
       <curve color="#ff7f0e" name="/controller_manager/statistics/values/controller_manager.write_time"/>
      </plot>
     </DockArea>
     <DockArea name="Controllers">
      <plot flip_x="false" flip_y="false" line_width="1.0" mode="TimeSeries">
       <range bottom="-1226.169025" left="51.220927" right="101.196346" top="50279.902025"/>
       <limitY/>
       <curve color="#f14cc1" name="/controller_manager/statistics/values/sleepy_controller.stats/execution_time/current_value"/>
       <curve color="#9467bd" name="/controller_manager/statistics/values/position_controller.stats/execution_time/current_value"/>
       <curve color="#17becf" name="/controller_manager/statistics/values/joint_state_broadcaster.stats/execution_time/current_value"/>
       <curve color="#bcbd22" name="/controller_manager/statistics/values/joint2_position_controller.stats/execution_time/current_value"/>
       <curve color="#1f77b4" name="/controller_manager/statistics/values/joint1_position_controller.stats/execution_time/current_value"/>
       <curve color="#d62728" name="/controller_manager/statistics/values/forward_position_controller.stats/execution_time/current_value"/>
      </plot>
     </DockArea>
     <DockArea name="Hardware">
      <plot flip_x="false" flip_y="false" line_width="1.0" mode="TimeSeries">
       <range bottom="-1.969225" left="51.220927" right="101.196346" top="140.924225"/>
       <limitY/>
       <curve color="#1ac938" name="/controller_manager/statistics/values/RRBot.stats/read_cycle/execution_time/current_value"/>
       <curve color="#ff7f0e" name="/controller_manager/statistics/values/RRBot.stats/write_cycle/execution_time/current_value"/>
      </plot>
     </DockArea>
    </DockSplitter>
   </Container>
  </Tab>
  <currentTabIndex index="0"/>
 </tabbed_widget>
 <use_relative_time_offset enabled="1"/>
 <streaming_buffer_size value="50"/>
 <!-- - - - - - - - - - - - - - - -->
 <!-- - - - - - - - - - - - - - - -->
 <Plugins>
  <plugin ID="DataLoad CSV">
   <parameters delimiter="0" skip_rows="0" time_axis=""/>
  </plugin>
  <plugin ID="DataLoad MCAP"/>
  <plugin ID="DataLoad ROS2 bags">
   <use_header_stamp value="false"/>
   <discard_large_arrays value="true"/>
   <max_array_size value="100"/>
   <boolean_strings_to_number value="true"/>
   <remove_suffix_from_strings value="true"/>
   <selected_topics value=""/>
  </plugin>
  <plugin ID="DataLoad ULog"/>
  <plugin ID="Foxglove ROS2 Bridge">
   <foxglove_client clamp_large_arrays="0" max_array_size="500" url="ws://127.0.0.1:8765" use_timestamp="0">
    <topics/>
   </foxglove_client>
  </plugin>
  <plugin ID="PlotJuggler Bridge">
   <websocket_client clamp_large_arrays="0" max_array_size="500" url="ws://127.0.0.1:9090" use_timestamp="0">
    <topics/>
   </websocket_client>
  </plugin>
  <plugin ID="ROS2 Topic Subscriber">
   <use_header_stamp value="false"/>
   <discard_large_arrays value="true"/>
   <max_array_size value="100"/>
   <boolean_strings_to_number value="true"/>
   <remove_suffix_from_strings value="true"/>
   <selected_topics value="/controller_manager/introspection_data/names;/controller_manager/introspection_data/values;/controller_manager/statistics/names;/controller_manager/statistics/values"/>
  </plugin>
  <plugin ID="UDP Server"/>
  <plugin ID="WebSocket Server"/>
  <plugin ID="ZMQ Subscriber"/>
  <plugin ID="CSV/Parquet Exporter"/>
  <plugin ID="Fast Fourier Transform"/>
  <plugin ID="Quaternion to RPY"/>
  <plugin ID="Reactive Script Editor">
   <library code="--[[ Helper function to create a series from arrays

 new_series: a series previously created with ScatterXY.new(name)
 prefix:     prefix of the timeseries, before the index of the array
 suffix_X:   suffix to complete the name of the series containing the X value. If [nil], use the index of the array.
 suffix_Y:   suffix to complete the name of the series containing the Y value
 timestamp:   usually the tracker_time variable

 Example:

 Assuming we have multiple series in the form:

   /trajectory/node.{X}/position/x
   /trajectory/node.{X}/position/y

 where {N} is the index of the array (integer). We can create a reactive series from the array with:

   new_series = ScatterXY.new(&quot;my_trajectory&quot;)
   CreateSeriesFromArray( new_series, &quot;/trajectory/node&quot;, &quot;position/x&quot;, &quot;position/y&quot;, tracker_time );
--]]

function CreateSeriesFromArray( new_series, prefix, suffix_X, suffix_Y, timestamp )

  --- clear previous values
  new_series:clear()

  --- Append points to new_series
  index = 0
  while(true) do

    x = index;
    -- if not nil, get the X coordinate from a series
    if suffix_X ~= nil then
      series_x = TimeseriesView.find( string.format( &quot;%s.%d/%s&quot;, prefix, index, suffix_X) )
      if series_x == nil then break end
      x = series_x:atTime(timestamp)
    end

    series_y = TimeseriesView.find( string.format( &quot;%s.%d/%s&quot;, prefix, index, suffix_Y) )
    if series_y == nil then break end
    y = series_y:atTime(timestamp)

    new_series:push_back(x,y)
    index = index+1
  end
end

--[[ Similar to the built-in function GetSeriesNames(), but select only the names with a give prefix. --]]

function GetSeriesNamesByPrefix(prefix)
  -- GetSeriesNames(9 is a built-in function
  all_names = GetSeriesNames()
  filtered_names = {}
  for i, name in ipairs(all_names)  do
    -- check the prefix
    if name:find(prefix, 1, #prefix) then
      table.insert(filtered_names, name);
    end
  end
  return filtered_names
end

--[[ Modify an existing series, applying offsets to all their X and Y values

 series: an existing timeseries, obtained with TimeseriesView.find(name)
 delta_x: offset to apply to each x value
 delta_y: offset to apply to each y value

--]]

function ApplyOffsetInPlace(series, delta_x, delta_y)
  -- use C++ indices, not Lua indices
  for index=0, series:size()-1 do
    x,y = series:at(index)
    series:set(index, x + delta_x, y + delta_y)
  end
end
"/>
   <scripts/>
  </plugin>
  <plugin ID="ROS2 Topic Re-Publisher"/>
 </Plugins>
 <!-- - - - - - - - - - - - - - - -->
 <previouslyLoaded_Datafiles/>
 <previouslyLoaded_Streamer name="ROS2 Topic Subscriber"/>
 <!-- - - - - - - - - - - - - - - -->
 <customMathEquations/>
 <snippets/>
 <colorMaps/>
 <!-- - - - - - - - - - - - - - - -->
</root>
